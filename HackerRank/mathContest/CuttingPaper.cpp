#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define INF 1e18

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    ll n, m;
    cin >> n >> m;
    cout << (n * m) - 1 << endl;
}
